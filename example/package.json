{
  "name": "playwright-performance-reporter-example",
  "version": "1.0.0",
  "description": "Example project for playwright-performance-reporter",
  "scripts": {
    "test": "cd .. && npm install && npm run build && cd example && playwright test"
  },
  "author": {
    "name": "Nikita Trotner",
    "email": "ntrotner@users.noreply.github.com",
    "url": "https://github.com/ntrotner"
  },
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.1"
  }
}
